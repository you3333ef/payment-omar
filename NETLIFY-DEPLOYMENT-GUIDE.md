# 🚀 دليل النشر التلقائي على Netlify

## 📋 متطلبات النشر

✅ **ملف `netlify.toml` مُعدّ مسبقاً**
✅ **مستودع GitHub متصل:** `you3333ef/payment-omar`
✅ **فرع النشر:** `main`

---

## 🔧 خطوات الإعداد

### الخطوة 1: إنشاء حساب Netlify

1. اذهب إلى: **https://netlify.com**
2. اضغط **"Sign up"**
3. اختر **"GitHub"** للتسجيل (أسهل للمزامنة)
4. فعل حسابك عبر GitHub

---

### الخطوة 2: إنشاء موقع جديد من GitHub

1. بعد تسجيل الدخول، اضغط **"Add new site"**
2. اختر **"Import an existing project"**
3. اضغط **"Deploy with GitHub"**
4. **أذّن Netlify** للوصول إلى GitHub (إذا طُلب)
5. ابحث عن مستودع: **`payment-omar`** أو **`you3333ef/payment-omar`**
6. اضغط على المستودع

---

### الخطوة 3: إعدادات البناء

**الصفحة ستظهر إعدادات البناء، تأكد من:**

| الحقل | القيمة | الحالة |
|-------|---------|---------|
| **Base directory** | (اتركه فارغاً) | ✅ |
| **Build command** | `npm run build` | ✅ |
| **Publish directory** | `dist` | ✅ |
| **Production branch** | `main` | ✅ |

**اضغط "Show advanced" → "New variable" لإضافة:**

```env
NODE_VERSION=18
```

---

### الخطوة 4: نشر الموقع

1. اضغط **"Deploy site"**
2. انتظر انتهاء البناء (~2-3 دقائق)
3. ستحصل على رابط مثل: `https://amazing-site-123456.netlify.app`

---

### الخطوة 5: تفعيل النشر التلقائي

✅ **مُفعّل تلقائياً!**

بمجرد ربط المستودع، كل `git push` إلى فرع `main` سيُطلق نشر تلقائي.

---

## 🔄 آلية النشر التلقائي

```bash
# بمجرد رفع تغييراتك إلى GitHub:
git add .
git commit -m "تحديث المشروع"
git push origin main

# Netlify سيقوم تلقائياً بـ:
# 1. سحب الكود الجديد
# 2. تشغيل npm run build
# 3. نشر مجلد dist
# 4. إرسال رابط النشر الجديد
```

---

## 📊 إعدادات Netlify الحالية

### `netlify.toml` محمّل مسبقاً:

```toml
[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
```

---

## 🔗 روابط مفيدة

- **لوحة تحكم Netlify:** https://app.netlify.com
- **مستودع GitHub:** https://github.com/you3333ef/payment-omar
- **حالة النشر:** https://app.netlify.com/sites/[اسم-الموقع]/deploys

---

## ⚙️ إدارة الموقع

### تحديث إعدادات الموقع:
1. اذهب إلى: https://app.netlify.com
2. اختر موقعك
3. اضغط **"Site settings"**

### عرض سجلات النشر:
1. في لوحة التحكم، اضغط **"Deploys"**
2. ستجد تاريخ جميع النشرات

### نشر يدوي:
- في صفحة "Deploys"، اضغط **"Trigger deploy"** → **"Deploy site"**

---

## 🐛 حل المشاكل

### مشكلة: فشل البناء
**الحل:**
- اذهب إلى "Deploys" → آخر نشر
- اضغط على "Deploy log"
- ابحث عن الأخطاء (عادة مشاكل dependencies)
- أصلح الأخطاء وأعد الرفع

### مشكلة: الشاشة البيضاء
**الحل:**
- ✅ **مُصلح مسبقاً!** ملف `netlify.toml` يحتوي على إصلاح SPA routing

### مشكلة: صفحة 404 عند تحديث الصفحة
**الحل:**
- ✅ **مُصلح مسبقاً!** إعدادات `[[redirects]]` صحيحة

---

## 📱 إعدادات إضافية (اختيارية)

### تخصيص اسم الموقع:
1. Site settings → Change site name
2. اختر اسماً مثل: `payment-omar-production`

### ربط نطاق مخصص:
1. Site settings → Domain management
2. اضغط "Add custom domain"
3. أدخل نطاقك (مثال: `payment.yoursite.com`)

### تفعيل Netlify Forms:
- ✅ **مُفعّل مسبقاً!** الـ forms موجودة في `index.html`
- اذهب إلى Site settings → Forms لإدارة الإشعارات

---

## ✅ فحص بعد النشر

### تأكد من:
- [ ] الموقع يحمل بشكل صحيح
- [ ] صفحات `/pay/*` تعمل
- [ ] صفحات `/r/*` تعمل
- [ ] لا توجد أخطاء في Console (F12)
- [ ] Service Worker يعمل

### اختبار سريع:
```
افتح: https://[اسم-الموقع].netlify.app
اذهب إلى: /pay/test123
تأكد من عمل React Router
```

---

## 🎉 تم!

**مبروك! موقعك الآن منشور تلقائياً على Netlify** 🚀

كلما رفعت تغييرات إلى GitHub، سينشر Netlify تلقائياً!
